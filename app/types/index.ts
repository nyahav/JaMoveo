export interface SongData {
    title: string;
    artist?: string;
    url: string;
  }
  
  export interface SongDetails {
    title: string;
    artist: string;
    lyrics: string;
    chords: string;
  }